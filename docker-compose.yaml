version: '3.9'

services:
  nextjs-app:
    image: nextjs-app:latest
    build:
      context: .
    ports:
      - "3000:3000"
    volumes:
      - .env.local:/app/.env.local
      - ./data:/app/data
      - ./documents:/app/documents
    environment:
      NODE_ENV: production